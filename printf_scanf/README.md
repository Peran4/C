Custom implementations of printf and scanf:
printf supports the following data types:
        %d - integers
        %f - floats
        %s - strings
        %p - point structure
scanf handles the following data types:
        %d - integers
        %f - floats
        %p - point structure (formatted as x y)
